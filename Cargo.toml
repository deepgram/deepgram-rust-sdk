[package]
name = "deepgram"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
bytes = "1.1.0"
futures = "0.3.21"
thiserror = "1.0.30"
# put this behind a feature
tokio = { version = "1.17.0", features = [ "full" ] }
# put this behind a feature
tokio-util = { version = "0.7.1", features = [ "codec" ] }
